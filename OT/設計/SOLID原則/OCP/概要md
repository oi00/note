# OCP:開放閉鎖原則

## **OCP（Open–Closed Principle）とは何か**

OCPとは次の原則です。

> ソフトウェアは「拡張に開いており、修正に閉じているべきである」
> 

これは

「既存コードを変更せずに、新しい振る舞いを追加できるように設計する」

という設計上の指針を示しています。

### **一文で説明するなら**

> OCPとは、
> 

> 「仕様追加という“予測できる変化”に対して、
> 

> 既存コードを修正せずに対応できるように設計する原則」であり、
> 

> 既存仕様の意味変更まで防ぐ原則ではない。
> 

## なぜ変更したくないのか？

- 変更によって既に依存しているモジュールが影響を受けるため
- 受けた影響はテストする必要があり、テスト範囲が広がるため

---

## **「修正に閉じている」の正しい意味**

OCPにおける「修正に閉じている」は、

- 既存コードを**一切変更しない**
- 将来のすべての変更に耐える

という意味ではありません。

正しくは次の意味です。

> 想定している“変更の種類”に対して、既存コードを修正しなくて済む状態にしておくこと
> 

つまり、OCPは

**どの変更を「追加」で扱い、どの変更を「修正」と割り切るかを選び取る原則**です。

---

## **変更には2種類ある**

OCPを理解するうえで重要なのは、変更を次の2種類に分けて考えることです。

### **1. 仕様追加（バリエーションの追加）**

- 税率の種類が増える
- 支払い方法が増える
- ルールのパターンが増える

👉 **OCPの主な適用対象**

この場合、

- 抽象（インターフェース・ポリモーフィズム）を用意し
- 実装を「追加」することで対応する

既存コードは修正せずに済む。

---

### **2. 既存仕様の意味変更（前提の変更）**

- ルールそのものの定義が変わる
- 抽象化した前提が成り立たなくなる
- ドメイン理解が更新される

👉 **OCPの適用外**

この場合、

- 既存の抽象自体が「嘘」になる
- 修正を避けることは原理的に不可能

無理に閉じ続けるべきではありません。

---

## **なぜOCPは「仕様追加」に効くのか**

OCPは、

- **変化しやすい部分**を「差し替え可能な実装」に押し込め
- **安定させたい部分**を修正しなくて済むように守る

ための原則です。

そのため、

- バリエーションが増える方向の変更
    
    → 抽象＋実装追加で吸収できる
    
- 意味が変わる方向の変更
    
    → 抽象の前提が壊れるため修正が必要
    

という差が生まれます。

[デザインパターンの多くはOCPを満たすための仕組みといえる](https://objectclub.jp/community/memorial/homepage3.nifty.com/masarl/article/dp-ocp.html)

デザインパターンは何を抽象化するのかのパターン

---

## **「既存仕様変更に弱い」のは欠陥ではない**

OCPが既存仕様の意味変更に対応できないのは、

- 原則の限界ではなく
- **現実の変更の性質を正しく反映している**

ためです。

意味が変わったにもかかわらず、

- 抽象を維持し続ける
- 無理に拡張で対応しようとする

これは設計として不健全です。

**意味が変わったなら、修正するべき**

そして、その修正がしやすいように

SRP・低結合・高凝集が重要になります。

---

## **OCPを適用する判断基準**

OCPは常に適用すべき原則ではありません。

| **状況** | **判断** |
| --- | --- |
| 変更が「種類の追加」になりそう | 適用する価値が高い |
| 変更が「意味の変更」 | 無理に守らない |
| 変更頻度が高い | 適用候補 |
| 将来の変化が不明確 | 早すぎる抽象化を避ける |

---

## **OCPの評価は「実装時」ではなく「変更時」に行われる**

### **一文で説明するなら**

**OCPとは、**

**「機能追加という実際の変更を通じて設計を検証し、**

**修正が必要だった場合に構造をリファクタリングするための判断基準」である。**

この説明ができれば、

OCPを「守る／破る原則」ではなく

**設計を成長させるための道具**として正しく伝えられます。

OCP（Open–Closed Principle）は、

**コードを書いた時点で守れているかどうかを断定できる原則ではない**。

OCPが本当に問うているのは、次の一点です。

> 「想定していた種類の機能追加が来たとき、既存コードを修正せずに対応できたか？」
> 

この問いは、**実際に機能追加が発生するまで答えが出ない**。

---

## **実装時にできるのは「仮説を立てる」ことまで**

実装時点では、

- このクラスは、将来どんな理由で変更されそうか
- その変更は「種類の追加」になりそうか

という**仮説**に基づいて設計しているだけであり、

「OCPを守れている」と確定することはできない。

つまり、

> OCPは事前に守るものではなく、
> 

> 事後に検証される設計原則
> 

である。

---

## **機能追加は「設計に対するテスト」**

機能追加が入ると、次のどちらかが起きる。

### **パターン1：既存クラスを修正せずに済んだ**

- 新しい実装を追加するだけで対応できた
- 既存クラスは一切変更しなかった

👉 この時点で

**「この変更に対してはOCPを守れていた」と分かる**

---

### **パターン2：既存クラスに修正が入った**

- if / switch を増やした
- 既存メソッドを書き換えた

👉 この時点で

**「この変更に対してはOCPを守れていなかった」と分かる**

これは失敗ではなく、

**設計仮説が現実とズレていたことが分かった**だけ。

---

## **OCP違反に気づいた後にやること**

ここで行うべきなのがリファクタリングである。

- 機能は変えない
- 構造だけを見直す
- 次からは「追加」で対応できる形にする

具体的には、

- 責務の分離
- 抽象の導入
- 依存関係の整理

といった**設計レベルのリファクタリング**を行う。

---

## **この流れが意味する設計プロセス**

整理すると、OCPは次のサイクルで扱う原則である。

1. シンプルに実装する
2. 機能追加を行う
3. 既存コードに修正が入ったかを確認する
4. 修正が入った理由を分析する
5. 必要なら構造をリファクタリングする

このプロセスを通じて、

> 設計は「仮説 → 検証 → 更新」されていく
> 

---

## **よくある誤解への補足**

- OCP違反 ＝ 悪い設計
    
    ではない
    
- 最初からOCPを完璧に守る
    
    必要はない
    

重要なのは、

> OCP違反に気づけることと、
> 

> それを次に活かせること
> 

である。